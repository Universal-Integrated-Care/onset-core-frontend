export declare type AppointmentApiProps = {
    params: Promise<{
      id: string;
    }>;
  };
  
  
export declare type PractitionerApiProps = {
    params: Promise<{
      practitionerId: string;
    }>;
  };
  
  export declare type ClinicUserIdApiProps = {
    params: Promise<{
      userId: string;
    }>;
  };
  
  
  export declare type ClinicApiProps = {
    params: Promise<{
      clinicId: number;
    }>;
  };